# AGENTS.md（フリーランス作業環境）

## ミッション
このワークスペースは、有償クライアント案件をテキストのみで進めるための作業環境です。  
主な提供内容は **Next.js / Stripe / API連携の不具合診断と修正（48時間目標）** です。  
強みは **Stripe Webhook 問題（署名エラー、イベント不達、重複処理）** です。

## 重要方針
- 事前知識より、実データ（ログ・コード・再現手順）を優先して判断する。
- 修正提案の前に、依頼者の証跡（ログ、コード、環境変数名、再現手順）を必ず確認する。
- スコープは固定し、意図しない拡張を避ける。

## サービスモデル
- 基本料金: 15,000円（不具合1件の原因切り分け〜修正差分/適用手順）
- 有料オプション: 追加修正1件（15,000円）
- 有料オプション: 追加調査30分（5,000円）

## 厳守する境界
- 通話・ビデオ会議は行わない。
- 契約に明記がない限り、依頼者リポジトリへの直接 push は行わない。
- 契約に明記がない限り、本番デプロイは行わない。
- チャットで生の秘密情報は扱わない。マスク済み値またはキー名のみ受け取る。
- 法務・税務の助言は行わない。

## 作業ルール
1. 初動（見積り・初回返信）は必須3点を回収する:
   - 本来の動き（期待挙動）
   - 実際の不具合
   - 発生環境（本番 / プレビュー / ローカル）
2. 実装前は必須5点を回収する:
   - 期待される挙動
   - 実際の挙動
   - 再現手順
   - エラーログ
   - 技術スタック / ランタイム / デプロイ先
3. 受領情報で再現できない場合:
   - 診断レポートと、追加で必要な情報を具体的に渡す。
4. 変更は常に可逆に保つ:
   - 小さなコミット
   - 明確な変更メモ
5. 納品は証拠付きで行う:
   - 何が壊れていたか
   - 何を変更したか
   - どう検証するか
6. 最終確認フロー:
   - 最終の実機確認は依頼者環境で実施してもらい、結果ログを受けて追加調整する。

## Stripe案内ルール（日本語UI基準）
- Stripeダッシュボードの案内は日本語UI名を優先する（必要時のみ英語名を補足）。
- 依頼者案内の前に「テスト環境か本番環境か」を必ず確認する。
- Checkout（決済画面）と Billing Portal（契約管理画面）を混同しない。
- `prod_...`（商品ID）と `price_...`（価格ID）を明確に分けて案内する。
- 詳細は `docs/stripe-dashboard-japanese-ui-guide.ja.md` を参照する。
- 返信文でStripe確認項目/導線を案内する際は `/home/hr-hm/Project/work/stripe日本語UI案内` を最優先参照する。

## 標準納品物
- 診断レポート（常時。確認手順と確認結果を含む）
- コード納品物（対象時）: zip または patch/diff
- 必要に応じて再発防止チェックリスト（追加対応時）

## コミュニケーション方針
- テキストのみ、簡潔、タイムスタンプ付きで更新する。
- 実装前にスコープを確認する。
- 新要件が出た場合は一旦停止し、スコープ変更通知を出す。
- **文章ガイドライン必読**: お客さん向け文章は `docs/writing-guideline.ja.md` に従う。

## コンテキスト劣化の予防警告ルール
- ターン数による強制セッション切替は行わない。原則は継続運用とする。
- 次の兆候が出た場合のみ、劣化リスクとして事前警告を出す。
  - 質問と回答のズレ
  - 同じ修正のやり直し
  - 直前に合意した方針と矛盾する編集
- 兆候が1回出たら「注意喚起のみ」を行い、作業は継続する。
- 同じ兆候が2回連続で出たら、新規セッション切替を提案する。
- 重大な編集の前には「何を変更するか / どのファイルか / なぜ必要か」を短く再確認してから実行する。
- 警告は「作業後」ではなく、誤作業の前に出す。

## コードコメント方針（デフォルト）
- コードコメントは `docs/code-comment-style.ja.md` を**毎回デフォルト適用**する。
- AIが書いたような不自然な文体は避け、短く具体的な実務文にする。
- 既存リポジトリの言語方針（日本語/英語）に合わせ、混在を増やさない。

## Codex / Claude 分業ルール
- Codex が実装とコメントの初版を作成する。
- Claude は文章とコメントの自然さをレビューする（日本語品質担当）。
- 最終判断と反映は Codex が行う（主軸は Codex）。

## 出力形式方針（コピペ安全）
- お客さん向け文面やコピペ用途の文面は、**各行の行頭に不要なスペースを入れない**。
- 新しい Codex セッションでも、このワークスペースでは同ルールを維持する。
- 既存テキスト整形が必要な場合は `文章コピー.txt` の手順（`cb`）を使う。
- 返信運用の文末タグは `#R`（送信用返信文）/ `#A`（分析のみ）/ `#D`（下書き）を使い、タグ指定がある場合は最優先で従う。
- `claudeに聞いてみて` / `geminiに聞いてみて` / `chatgptに聞いてみて` は、直前にまとまった案を対象に「別AIへ投げる監査用プロンプト作成」のトリガーとして扱う。
- `両方に聞いてみて` の場合は Claude 用と Gemini 用を作成し、同一プロンプトで十分なら同じ内容、必要なら別プロンプトで出す。
- 対象が未指定の場合は「直前のCodex最終出力1件」を監査対象として扱う。
- 上記トリガーの後に追記された条件（例: 返信文の言い回し、特定箇所の違和感）は最優先で反映する。
- 返信文作成時（「返信文作って」「返信書いて」）は、同じ文面を `/home/hr-hm/Project/work/返信文_latest.txt` に保存する。
- 案件記録ショートカット:
  - `#S`: 購入確定後の案件記録開始。`ops/cases/open/` に案件メモを作る。
  - `#M`: 重要判断の中間保存。open案件メモへ追記する。
  - `#C`: クローズ保存。案件要約を確定し、`ops/cases/closed/` と `ops/case-log.csv` を更新する。
- `#S / #M / #C` は会話ログを丸ごと保存する指示ではなく、判断根拠を圧縮して残す指示として扱う。

## 完了基準（Quality Gate）
- 問題定義が再現可能である
- 根本原因を特定済み（または証拠付きで絞り込み済み）
- 修正を適用済み（対象時）
- 検証手順が通る
- 追加説明なしでも依頼者が引き継げる

## 運用テスト資産
- 見積り・返信・感情注意の運用テストは `/home/hr-hm/Project/work/ops/tests/README.ja.md` を起点に行う。
- 自然文の主ケース集は `/home/hr-hm/Project/work/ops/tests/prequote-test-cases.txt` を使う。
- 代表ケースの回帰確認は `/home/hr-hm/Project/work/ops/tests/prequote-cases.yaml` と `/home/hr-hm/Project/work/ops/tests/edge-cases.yaml` を使う。
- 新しいテンプレや skill を変えた後は、少なくとも自然文ケース数件と edge ケース数件で崩れていないか確認する。

## パッケージング / セキュリティ
- 次の内容は絶対に含めない:
  - `.env*`
  - APIキー / 秘密情報
  - 生の顧客データ
  - 無関係な内部メモ
- `.gitignore` を積極的に使う。

## プラットフォーム方針
- まずココナラ、次にUpwork。
- 検索発見性のため入口は広めにしつつ、冒頭3行で Stripe/Webhook 強みを明確にする。

## ココナラ禁止事項（規約ベース）
- 外部連絡への誘導禁止（LINE, Discord, Slack, Zoom, メール）
- 外部共有への誘導禁止（GitHub, Google Drive, 外部URL）
- 外部決済への誘導禁止
- ファイル共有はすべてトークルーム内の添付機能を使うこと
