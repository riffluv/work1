---
name: delivery-pack-ja
description: "ココナラ案件の納品物を、bugfix は `00_結論と確認方法.md` とコード納品物、handoff は `00_結論と要点.md` と `01_[対象フロー名]_引き継ぎメモ.md` を標準として作成する。正式納品前チェックと正式納品メッセージ作成を依頼されたときに使う。"
---

# 納品パック作成（日本語）

## 目的
- 納品物の粒度を固定し、差し戻しを減らす。
- 非エンジニアにも伝わる納品説明を作る。
- 秘密情報を含まない納品を徹底する。

## 参照
- テンプレート: `references/pack-templates.ja.md`
- 納品前ガード: `references/delivery-checklist.ja.md`
- handoff テンプレート: `/home/hr-hm/Project/work/docs/handoff-delivery-template.ja.md`
- 運用正本: `/home/hr-hm/Project/work/docs/coconala-guide-market-ops.ja.md`
- Scope Snapshot: `/home/hr-hm/Project/work/ops/common/scope-snapshot-template.md`
- サービス定義: `/home/hr-hm/Project/work/ops/services/next-stripe-bugfix/service.yaml`

## 入力（最低限）
1. 対象サービス（`bugfix-15000` / `handoff-25000`）
2. 今回の対象
3. 合意済み納品形式
4. 依頼者側で実施する確認手順

### bugfix 追加入力
1. 原因の要約（何が壊れていたか）
2. 修正内容の要約（どこをどう直したか）

### handoff 追加入力
1. 対象フロー名
2. 危険箇所の要約
3. 次の着手順の要約

## 出力
### bugfix
1. `00_結論と確認方法.md`（冒頭に `今回の対象 / 結論 / 確認手順` を置く）
2. 修正済みファイル（標準）または修正差分（任意）
3. Scope Snapshot（内部確認用。購入者向け別ファイルにはしない）
4. 正式納品メッセージ（そのまま送信可能）

### handoff
1. `00_結論と要点.md`
2. `01_[対象フロー名]_引き継ぎメモ.md`
3. Scope Snapshot（内部確認用。購入者向け別ファイルにはしない）
4. 正式納品メッセージ（そのまま送信可能）

## 作成フロー
1. 納品形式の合意を確認する（未合意なら「標準は修正済みZIP、希望があればpatch併記」と案内する）。
2. 対象サービスを確認する。
3. `bugfix-15000` なら `pack-templates` で標準2点セットを埋める。
4. `handoff-25000` なら `/home/hr-hm/Project/work/docs/handoff-delivery-template.ja.md` を使い、`00_結論と要点.md` と `01_[対象フロー名]_引き継ぎメモ.md` を作る。
5. `delivery-checklist` で秘密情報と説明不足を検査する。
6. 正式納品メッセージを作る（承諾/差し戻し案内を含める）。

## ガード
- `.env` 値、APIキー値、Webhook秘密値を含めない。
- 納品物は完成品として作る。本文内で結果共有・追加情報送付・継続前提の依頼をしない。
- 依頼者に求めるのは最終確認のみとし、合意内容との相違は正式納品メッセージで差し戻し案内する。
- `japanese-chat-natural-ja` を通すのは正式納品メッセージなどトークルーム文面だけで、添付する納品物本文には適用しない。
- 外部レビューや実務で得た改善は、`reply-only` / `common` / `delivery-only` を判定してから反映する。
- 納品物正本へ入れてよいのは `delivery-only` または `common` だけで、`reply-only` をそのまま流用しない。
- `bugfix-15000` の `00_結論と確認方法.md` は `今回の対象 -> 結論 -> 確認手順 -> 原因 -> 修正内容` の順を基本とする。
- `handoff-25000` の納品物は `00_結論と要点.md` と `01_[対象フロー名]_引き継ぎメモ.md` を標準にし、抽象語だけで終わらせない。
- 依頼者が次にやることを1行で明記する。
- bugfix は冒頭3行で「直ったか / 原因 / 確認方法」が読めるようにする。
- handoff は冒頭で「今回見たフロー / 危険箇所上位 / 次の1手」が読めるようにする。
- 見出しは社内技術文書より購入者目線を優先する。
- patchを併記する場合は「通常は修正済みファイルを差し替え、patchはGit運用者向け」と明記する。
- 正式納品文には「承諾/差し戻しの選択」と「約3日間未操作で自動承諾クローズ」の案内を入れる。
- `差し戻しは原則1回` は必要時に補足し、初回納品文で圧をかけない。
- 評価依頼を入れる場合は1回のみ、任意トーンで短く書く。
- 未提供/提供途中の状態では正式納品メッセージを作らず、状況確認またはキャンセル案内に切り替える。
- Scope Snapshot を先に埋め、合意した範囲を再掲する。
- 添付前にメタデータと不要ファイルの混入を確認する。

## 仕上げチェック
- bugfix は `今回の対象 / 結論 / 確認手順` だけで概要が伝わるか。
- handoff は `今回見たフロー / 危険箇所上位 / 次の1手` だけで概要が伝わるか。
- bugfix では「何が原因だったか」が1〜3行で読めるか。
- handoff では `確認できたこと / 推定 / 未確認` が章で分かれているか。
- bugfix は変更点がファイル単位で追えるか。
- handoff は対象フロー名入りの `01_[対象フロー名]_引き継ぎメモ.md` になっているか。
- 確認手順が依頼者環境前提で書かれているか。
- 納品物本文に `共有してください` `教えてください` など継続前提の依頼が残っていないか。
- 差し戻し案内を納品物本文へ書かず、正式納品メッセージに寄せているか。
- 納品形式（ZIP/patch）と優先適用方法が明記されているか。
- 正式納品メッセージに差し戻し案内があるか。
- 正式納品メッセージに「約3日間未操作で自動承諾クローズ」案内があるか。
- Scope Snapshot が現在の合意内容と一致しているか。
- 添付前チェックでメタデータと不要ファイルを見ているか。
