---
name: coconala-reply-ja
description: "ココナラ向けの自然な日本語返信を作る。相手文の貼り付けだけなら分析のみを返し、「返信文作って」等の明示がある場合のみ送信用文面を作成する。"
---

# ココナラ返信（日本語）

## この skill を使う場面
- ユーザーが「なんて返す？」「返信文作って」「返信相談」と依頼したとき
- ココナラ上でお客さんに送る日本語メッセージを作るとき
- AIっぽさを抑えて、短く自然な文面にしたいとき

## モード判定（重要）
- 相手文の貼り付けのみ（または質問のみ）: **分析のみ**を返す
- 「返信文作って」「返信書いて」「送信用で」「そのまま送れる文」などの明示あり: **送信用文面**を返す
- 「分析だけ」「分析のみ」明示: **分析のみ**を返す
- 判定が曖昧な場合: まず分析のみを返し、必要なら最後に「返信文も作れます」と1行添える

## 想定入力フォーマット
どちらでも受け付ける。足りない項目は安全側で補完する。

1行:
`返信相談: 相手文=... / 相談点=... / 希望=...`

複数行:
`返信相談`
`相手文: ...`
`相談点: ...`
`希望: ...`

## 分析のみの出力形式
- 要点（相手の主張）
- 懸念（技術/運用/規約）
- 追加で確認すべき項目（2〜4個）
- 推奨対応（次アクション）

## 返信文作成フロー
1. `相手文` `相談点` `希望` を抽出する。
2. 返信の目的を先に決める（回答 / 追加質問 / スコープ調整 / クローズ案内）。
3. そのまま貼れる本文を1本作る。
4. 返信文モードでは、本文を `/home/hr-hm/Project/work/返信文_latest.txt` に保存する。
5. リスクやスコープ変更がある場合は、次アクションを1行で明記する。
6. ユーザー指定がない限り短めにまとめる。

## 出力ルール
- 行頭に不要なスペースを入れない。
- ユーザー指定がない限り、コードブロックを使わない。
- `です・ます` を基本にしつつ、チャットのテンポを維持する。
- 結論 → 理由 → 次アクション/依頼 の順で書く。
- `本日中` `再現手順` `エラーログ` など具体語を優先する。
- 抽象的でAIっぽい定型文を避ける。

## ココナラ運用の禁止ガード
- 外部連絡手段（LINE/Discord/Zoom/メール）へ誘導しない。
- 外部共有（GitHub/Drive/外部URL）へ誘導しない。
- `.env` 値、APIキー値、Webhook秘密値など生の秘密情報を要求しない。
- スコープ外は、追加対応として明確に切り分ける。

## 文体の参照
- 文体・言い換え: `references/style-rules.ja.md`
- 返信の型: `references/message-patterns.ja.md`

## 仕上げ前チェック
- 相手の質問に正面から答えているか
- 双方の次アクションが明確か
- 禁止事項（外部誘導/秘密情報要求）を含んでいないか
- 読んで不自然な過剰敬語になっていないか
