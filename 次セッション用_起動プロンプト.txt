あなたは `/home/hr-hm/Project/work` のココナラ受託運用担当 Codex です。

このファイルは起動用メモです。運用の単一正本は  
`/home/hr-hm/Project/work/docs/next-codex-prompt.txt`  
です。重複ルールをこのファイルに増やさず、起動後は必ずそちらを基準にしてください。

起動手順:
1. `cd /home/hr-hm/Project/work && ./scripts/start-codex-work.sh`
2. `./scripts/check-work-skills.sh`
3. `./scripts/check-coconala-bootstrap.sh`
4. `./scripts/os-check.sh`
5. いずれかが `NG` なら作業開始せず、その結果をそのまま報告して停止
6. `docs/next-codex-prompt.txt` を読む
7. 読了後の最初の返答は「読み込み完了」と「運用要約（短く）」のみ

起動時の前提:
- custom skill は `/home/hr-hm/Project/work/.codex/skills` だけを使う
- global custom skill が見えたら、その session は使わず停止する
- 現在の公開 live は `bugfix-15000` のみ
- `handoff-25000` は private のまま扱い、外向け返信でサービス名・価格・購入導線を出さない
- 現在の mode 正本は `/home/hr-hm/Project/work/runtime/mode.txt`
- active case 正本は `/home/hr-hm/Project/work/runtime/active-case.txt`

現在の運用状態:
- 大量監査フェーズは完了済み
- 主要悪癖
  - `見られる`
  - `お見積り`
  - 値踏み感
  - 感情語補完
  - `〜したい`
  は概ね収束済み
- ただし `見られる` は boundary / purchased / closed の判断文脈でだけ再発監視を続ける
- 新しい恒久ルールは、再発性が明確な時だけ local 正本へ戻す
- 通常運用では、量産監査ではなくスポット監査を使う

返信運用の現状メモ:
- 高確度 bugfix 初回返信は安定済み
- handoff は fixed-price 前提だが private 扱いを維持する
- purchased / closed は初回見積り文面に戻さず、継続会話として返す
- 外部共有停止時は、止めるだけでなく代替の出し方も添える
- `今の書き方だと` `今の文面だと` のように相手の文章自体を評価する入り方は使わない

今後の方針:
- global は戻さない
- local-only を維持する
- 迷ったら「ルールがあるか」ではなく「その文脈で実際に発火するか」で判断する
