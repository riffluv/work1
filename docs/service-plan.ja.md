# ココナラ サービス設計（現行）

更新日: 2026-04-29

## 結論（現行の固定モデル）

プラン3段ではなく、以下の「基本 + オプション」で運用する。

機械判定の正本は `/home/hr-hm/Project/work/os/core/service-registry.yaml` -> `facts_file` -> `service.yaml` とし、この文書は運用メモとして扱う。

1. 基本料金: 15,000円  
内容: 不具合1件の原因切り分けから修正内容の作成（修正済みZIPまたはpatch）まで

2. 有料オプションA: 追加修正1件（不具合1件） 15,000円

## 不具合1件の定義

- 同一原因に紐づく1つの現象（1フロー / 1エンドポイント）を1件とする
- 原因または対象フローが別の場合は追加修正として扱う

## 前提

- テキストのみ（通話なし）
- 納品は `00_結論と確認方法.md` + 修正済みファイル（ZIP・標準）または修正差分（patch・任意）
- 本番デプロイ、直接push、新機能開発は標準範囲外
- 秘密情報は値を受け取らず、キー名とマスク情報のみ受け取る
- 最終の実機確認は依頼者環境で実施し、当初合意した内容と相違が残る場合は差し戻しで確認する

## 追加料金の扱い

この項目は、サービス範囲ではなく取引中の支払い導線の運用メモ。
`bugfix` / `handoff` / `neither` の判定や成果物の分離は、各 service facts と `platform-contract.yaml` を優先する。

- 取引中の追加対応: 有料オプション追加で対応
- 正式納品後〜クローズ前: 必要時のみ、おひねり等の追加支払いで対応
- クローズ後: 新規依頼で対応
- 実務上は、追加費用や追加作業の合意を原則として正式納品前に終える
- 追加対応が可能な場合でも、自動追加料金・一括料金化・同日対応保証として案内しない
- 通常返信では、buyer が支払い方法を聞いていない限り `別対応として進め方と費用を相談` までに留める

## 運用メモ

- 見積り受付: ON
- 見積り必須: OFF（即購入も許可）
- 同時対応件数: 1件で固定し、品質優先で回す
- prequote の bugfix は、原則として購入後に作業開始する
- prequote では ZIP 非依存を標準にし、症状・環境・エラー文・スクショで入口判断する
- prequote の約束範囲は `/home/hr-hm/Project/work/docs/coconala-prequote-commitment-policy.ja.md` を参照する
