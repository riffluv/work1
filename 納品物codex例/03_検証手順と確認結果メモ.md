# 動作確認の手順と結果

案件名: Next.js + Stripe Webhook 500（本番のみ）
作成日: 2026-02-26

## 結論

こちらで確認した範囲では、Webhook署名検証エラーは解消しています。
以下の手順で、依頼者様環境でもご確認ください。

## 1. 確認の手順
1. StripeテストモードでCheckout決済を実行
2. Stripeダッシュボードで `checkout.session.completed` の配信結果を確認
3. サーバーログで署名検証エラーの有無を確認
4. DBの `users.plan` を確認

## 2. 正常に動いている状態
- Webhookイベントの配信結果が `Delivered`
- 署名検証エラーが出ない
- 対象ユーザーの `plan` が `pro` に更新される

## 3. こちらで確認した結果
- 決済実行: 成功
- Stripeイベント配信: Delivered
- サーバーログ: 署名検証エラーなし
- DB更新: `users.plan=pro` を確認

## 4. もし問題が残っている場合
- 失敗イベントの `evt_...`（2〜3件）
- 発生時刻（日本時間）
- サーバーログの該当箇所（30行程度）
- どの環境で発生したか（本番/プレビュー/ローカル）
