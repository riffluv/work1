# サービスページ

## 現在の前面ファイル

- `bugfix-15000.live.txt`
- `handoff-25000.ready.txt`

## 退避済み rehearsal 入力

想定メッセージの元ケース集は次に退避しています。

- `rehearsal-input/`

## 退避済み rehearsal 資産

2026-03-30 時点の草案、Claude/Gemini 監査 batch、横断サマリーは次に退避しています。

- `rehearsal/20260330/`

## 購入前見本まわり

- `送付文/購入前見本/` は、見積り相談時にそのまま貼る固定文の置き場です
- `送付文/正式納品/` は、正式納品時にそのまま貼る固定文の置き場です
- 購入前に見せる見本納品物は、送付文とは別階層で管理します
- `サービスページ/購入前見本/` は外向けの購入前見本です
- 実務で毎回参照する内部品質本丸は `/home/hr-hm/Project/work/docs/internal-quality-samples/` に分けています
- `bugfix-15000` と `handoff-25000` の購入前見本本体は `サービスページ/購入前見本/` にそろえます
- `bugfix-15000` の内部品質本丸は、購入前見本の元にする内部基準として別に残します

## 補足

- `次期サービス候補/` は候補段階の参考資料です
- `live` / `ready` は既存 docs や script から参照されているため、現時点ではこの階層に残しています
