# bugfix-15000 返信監査 batch

対象: `RE-2026-04-27-bugfix-24-prequote-specificity-after-shelf-r1`

目的:

- CHG-065 で `prequote 具体症状拾い` を renderer / validator / Gold 26 に最小反映した後の r1 確認
- r0 で確認された旧テンプレ三点セット回帰と purchased 進捗同義反復を、renderer / validator の最小補修後に再監査する
- batch-23 で直した4型そのものではなく、近いが未専用分岐の普通 prequote で旧テンプレ回帰が残るかを見る
- local lint OK でも、buyer の具体情報を拾わず `内容ありがとうございます / この不具合なら15,000円 / どこで止まっているか確認` へ戻っていないかを監査する
- purchased 進捗催促で `確認中です` 相当の同義反復に戻っていないかを見る
- closed 後の一般的な確認手順質問が、過剰に見積り導線へ寄らず、一般アドバイスとして自然に返せるかを見る
- `handoff-25000` や 25,000円側の外向け導線が出ていないか確認する

今回の構成:

- B01〜B05: 普通寄り prequote / 具体症状・範囲混在
- B06: purchased の進捗確認 + diff希望
- B07: closed 後の確認手順質問

監査で見てほしい点:

- 主質問に最初に答えているか
- buyer が書いた具体症状・技術情報・不安を1〜2個拾えているか
- 旧テンプレ三点セットへ戻っていないか
- 普通の prequote が、過剰な契約説明にも、雑な基本テンプレにも寄っていないか
- `15,000円 / 不具合1件 / 追加料金は勝手に発生しない / 未完成納品へ押し切らない` の軸が崩れていないか
- quote_sent / purchased / closed で、buyer が次に何をすればよいか迷子になっていないか
- `transaction_model_gap` を使う場合は、必要に応じて `transaction_clarity / phase_route_clarity / work_payment_boundary_clarity / buyer_not_lost / free_support_expectation_risk / responsibility_over_admission_risk / request_minimality` のどれが崩れているかを短く特定してください
- 文面がぶつ切り、丸引用、テンプレ反復、内部分類語、surface_overexposure になっていないか

戻し先の判定方針:

- `validator`: deterministic に再発し、機械的に落とせる事故
- `gold`: 判断順序として残す価値があり、テンプレ丸写しではなく参照 anchor にするもの
- `reviewer_prompt`: 常時本文に出すと長文化するが、監査レンズとして有効なもの
- `renderer`: 同じ r0 崩れが複数 batch で再発し、case_fix では止まらないもの
- `case_fix`: その case だけの文体・温度・言い換え
- `preference`: 好み差で、今回の batch 採用を止めるほどではないもの

出力形式:

1. まず一文で結論
   - `採用可 / 条件付き採用 / 要修正` のどれかを最初に明記してください
   - そのうえで、「今回はそのまま使ってよいか / どこを直してから使うべきか」を1〜2文で書いてください
2. 共通して良い点
   - 2〜4点まで
3. 危ない点 / 共通 drift
   - 本当に再発しそうなものだけ
   - 細かい好みより、実務リスクと buyer の違和感を優先してください
4. ケース単位の必須修正
   - 必須修正がなければ `必須修正なし` と明記してください
   - 必須 / 軽微 を分けてください
5. 学習判定
   - 必須 / 軽微 の指摘ごとに、必要な場合だけ短く添えてください
   - `直す単位`: `case_fix / pattern_candidate / rule_return_candidate / preference / reject`
   - `再発根拠`: `なし / 同batch内 / 過去batch同型 / 既存rule未反映疑い`
   - `既存rule hit`: `あり / なし / 不明`
   - `戻し先候補`: `batch / reviewer_prompt / gold / rule / renderer / service_facts / validator / none`
6. rule に戻すべき再発パターン
   - `reply-only / common` を分けてください
   - 再発性が弱いものは無理に rule 化しないでください
7. 採点
   - 必要なら `直答 / scope順守 / 情報節約 / 自然さ / 次アクション明確さ` を短く評価してください
8. 最後に短くまとめ
   - `今回は止めてよい / 1〜2点だけ直す / まだ危ない` のどれかを、人間向けに短くまとめてください

まだ `rule / validator / gold / renderer / reviewer_prompt` には反映しないでください。監査結果を見て、再発性があるものだけ最小反映します。

---

## B01

stock_id: STK-020
状態: prequote
主質問タイプ: 普通の見積り相談・プラン変更後未反映型
最初に答えるべき問い: Stripe では支払い完了しているのに、サイト側のプランが反映されない件を対応できるか。
選定意図: 非エンジニアが再現手順とログ不安を具体的に書いている場面で、旧テンプレへ戻らず症状を拾えるかを見る。
r0_local_lint: OK

相手文:

> お世話になります。マイページで「プラン変更」ボタンを押すと、Stripeの画面に遷移して支払いは完了するんですが、戻ってきたときにプランが変わっていません。ページをリロードしても変わらないです。F5しても同じで、翌日になっても反映されていません。ログはどこを見ればいいか分かりません。

返信候補:

ご相談ありがとうございます。

Stripeで支払いは完了しているのに、戻った後もサイト側のプランが反映されない状態ですね。
この不具合なら15,000円で対応できます。

ログの場所が分からない状態でも大丈夫です。まずは決済後の反映処理と、サイト側のプラン更新処理のつながりを確認します。

この内容で進める場合は、そのままご購入いただいて大丈夫です。必要情報がそろい次第、一次結果は48時間以内を目安にお返しします。

---

## B02

stock_id: STK-030
状態: prequote
主質問タイプ: 普通の見積り相談・本番だけ405型
最初に答えるべき問い: Next.js API Route の POST が本番 Vercel だけ 405 になる件を対応できるか。
選定意図: batch-22 の 405 型に近いが、まだ renderer 専用分岐ではないケースで旧テンプレ回帰を見る。
r0_local_lint: OK

相手文:

> Next.jsのAPIルートで、POSTリクエストが本番だけ405 Method Not Allowedになります。Stripe Checkoutのセッション作成のところです。デプロイ先はVercelです。ローカルだと普通にPOSTできます。

返信候補:

ご相談ありがとうございます。

本番のVercel上で、Stripe Checkoutのセッション作成APIだけが405になる状態ですね。
この不具合なら15,000円で対応できます。

まずは本番とローカルで、Next.js API Route のルーティングやPOSTの扱いに差が出ていないかを確認します。

この内容で進める場合は、そのままご購入いただいて大丈夫です。必要情報がそろい次第、一次結果は48時間以内を目安にお返しします。

---

## B03

stock_id: GMN-004
状態: prequote
主質問タイプ: 普通の見積り相談・preview環境Webhookエラー型
最初に答えるべき問い: ローカルは動くが preview 環境の Webhook でエラーが出る件を対応できるか。
選定意図: 環境差、Webhook、event ID、キー名が出ている相談で、具体情報を拾えるかを見る。
r0_local_lint: OK

相手文:

> ローカルは動くのにプレビュー環境だとWebhookでエラーが出ます。対象のイベントIDはevt_999999、キー名はSTRIPE_WEBHOOK_SECRETです。

返信候補:

ご相談ありがとうございます。

ローカルは動くのに、プレビュー環境のWebhookだけエラーになる状態ですね。
この不具合なら15,000円で対応できます。

共有いただいたEvent IDと STRIPE_WEBHOOK_SECRET の設定状況を起点に、プレビュー環境側のWebhook endpointと環境変数の反映を確認します。

この内容で進める場合は、そのままご購入いただいて大丈夫です。必要情報がそろい次第、一次結果は48時間以内を目安にお返しします。

---

## B04

stock_id: V5-004
状態: prequote
主質問タイプ: 普通の見積り相談・Webhook環境差/複数仮説型
最初に答えるべき問い: Vercel で Webhook 署名検証が 400 になる件を対応できるか。
選定意図: 環境変数、endpoint URL、Next.js 14→15 など仮説が複数ある場面で、情報の拾い方と見立ての絞り方を見る。
r0_local_lint: OK

相手文:

> api/webhookのRoute Handlerで署名検証してるんですが、ローカルだとOKなのにVercelだと400になります。STRIPE_WEBHOOK_SECRETはVercelの環境変数にも入れてます。stripe listenで転送してるのはローカルだけなので、本番のendpointURLが違うのかもしれないですが。あと関係あるか分からないけど、最近Next.js 14から15にアップデートしました。

返信候補:

ご相談ありがとうございます。

ローカルでは通るのに、Vercel上でWebhook署名検証が400になる状態ですね。
この不具合なら15,000円で対応できます。

まずはVercel側の環境変数、endpoint URL、Next.js 14から15への更新でRoute Handlerまわりの扱いが変わっていないかを中心に確認します。

この内容で進める場合は、そのままご購入いただいて大丈夫です。必要情報がそろい次第、一次結果は48時間以内を目安にお返しします。

---

## B05

stock_id: STK-073
状態: prequote
主質問タイプ: 普通の見積り相談・フロント問題/Stripe連携混在型
最初に答えるべき問い: フロントエンドのバグも対象か。管理画面ボタン不反応と Stripe 決済後ポップアップ不具合をどう扱うか。
選定意図: Stripe連携とフロント問題が混ざる場面で、対象可否と1件/別件の切り分けができるかを見る。
r0_local_lint: OK

相手文:

> 質問があります。Next.jsの不具合修正とのことですが、フロントエンドのバグも対象ですか？管理画面のボタンを押しても反応しないのと、Stripe決済後のポップアップが出ないのと、2つあります。フロントだけの問題ならお門違いかもしれませんが。

返信候補:

ご相談ありがとうございます。

Next.jsで動いているサイトであれば、フロント側の不具合も対応範囲に入ります。

ただ、管理画面のボタン不反応とStripe決済後のポップアップ不具合は、同じ原因か別の原因かを先に切り分けます。
同じ原因なら15,000円の範囲で見ます。別原因の場合は、勝手に追加作業へ進まず、対応方法と費用の有無をご相談します。

まずは一番困っている方を起点に確認します。Stripe決済後のポップアップを優先する場合は、この内容でご購入いただいて大丈夫です。

---

## B06

stock_id: STK-057
状態: purchased
主質問タイプ: 購入後・進捗確認 + diff希望型
最初に答えるべき問い: 今どこまで進んでいるか。修正方針を diff で見せてもらえるか。
選定意図: purchased 進捗催促で、同義反復に戻らず現在地と diff 希望への回答を出せるかを見る。
r0_local_lint: OK

相手文:

> 進捗確認です。あと、修正方針を共有いただく際にdiffで見せてもらえると助かります。うちのコードベースにどう影響するか把握したいので。

返信候補:

ありがとうございます。
お待たせしてすみません。まず状況を整理します。

現在は、対象処理のどこで止まっているかを確認しています。まだ原因は断定していません。

修正方針は、変更予定ファイルとdiffで影響範囲が分かる形でお送りします。

本日10:44までに、分かっている点と次に見る箇所をお送りします。

---

## B07

stock_id: STK-059
状態: closed
主質問タイプ: closed後・テスト環境なし確認手順質問型
最初に答えるべき問い: テスト環境がない場合、修正後確認はどうすればよいか。いきなり本番で試すしかないのか。
選定意図: closed 後でも、実作業ではなく一般的な確認手順として答えられる範囲を、過剰な見積り導線にせず返せるかを見る。
r0_local_lint: OK

相手文:

> 前回修正いただいた件でもう1つだけ。修正後のテストってどうやって確認すればいいですか？テスト環境がないんですが、いきなり本番で試すしかないですか？次にお願いするときに備えて知っておきたくて。

返信候補:

ご連絡ありがとうございます。
確認方法についてのご質問ありがとうございます。

いきなり本番だけで広く試すより、確認したい操作を絞って順に見る形が安全です。次回必要なら、その前提でも確認手順はお渡しできます。
