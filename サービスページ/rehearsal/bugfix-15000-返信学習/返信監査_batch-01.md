# bugfix-15000 返信監査 batch

対象: `RE-2026-04-27-bugfix-28-normal-boundary-routing-r1`

目的:

- Gold 29 追加後、普通寄り prequote で技術症状を拾えるかを継続確認する
- 非Stripe決済、緊急復旧、情報不足、テスト/本番切替など、汎用テンプレへ戻りやすい相談を混ぜる
- quote_sent / purchased の sentry で、本番反映手順、二重引き落とし、返金操作質問の境界が崩れないかを見る
- `handoff-25000` や 25,000円側の外向け導線が出ていないか確認する

今回の構成:

- B01: 普通寄り prequote / 技術情報が多いサブスク更新不具合
- B02: 普通寄り prequote / 非Stripe決済 scope 限定
- B03: 普通寄り prequote / 緊急復旧時間質問
- B04: 普通寄り prequote / 本番のみ timeout / 境界判断
- B05: 普通寄り prequote / テスト環境から本番切替後の不具合
- B06: purchased / 二重引き落としと返金操作質問
- B07: quote_sent / 修正ファイルの本番反映とVercel手順

監査で見てほしい点:

- 主質問に最初に答えているか
- buyer が書いた具体症状・技術情報・不安を1〜2個拾えているか
- 旧テンプレ三点セットへ戻っていないか
- 非Stripe決済名が出た時、決済サービス全体ではなく Next.js 側の Webhook/API 受信処理に範囲を閉じているか
- 緊急復旧や本番停止に対して、復旧時間を保証せず、一次結果目安と混同させていないか
- `.env` / APIキー / 秘密値の送付を求めていないか
- purchased で、返金操作の案内と返金実行代行を混同していないか
- buyer が次に何をすればよいか迷子になっていないか
- `transaction_model_gap` を使う場合は、必要に応じて `transaction_clarity / phase_route_clarity / work_payment_boundary_clarity / buyer_not_lost / free_support_expectation_risk / responsibility_over_admission_risk / request_minimality` のどれが崩れているかを短く特定してください
- 文面がぶつ切り、丸引用、テンプレ反復、内部分類語、surface_overexposure になっていないか

戻し先の判定方針:

- `validator`: deterministic に再発し、機械的に落とせる事故
- `gold`: 判断順序として残す価値があり、テンプレ丸写しではなく参照 anchor にするもの
- `reviewer_prompt`: 常時本文に出すと長文化するが、監査レンズとして有効なもの
- `renderer`: 同じ r0 崩れが複数 batch で再発し、case_fix では止まらないもの
- `case_fix`: その case だけの文体・温度・言い換え
- `preference`: 好み差で、今回の batch 採用を止めるほどではないもの

出力形式:

1. まず一文で結論
   - `採用可 / 条件付き採用 / 要修正` のどれかを最初に明記してください
   - そのうえで、「今回はそのまま使ってよいか / どこを直してから使うべきか」を1〜2文で書いてください
2. 共通して良い点
   - 2〜4点まで
3. 危ない点 / 共通 drift
   - 本当に再発しそうなものだけ
   - 細かい好みより、実務リスクと buyer の違和感を優先してください
4. ケース単位の必須修正
   - 必須修正がなければ `必須修正なし` と明記してください
   - 必須 / 軽微 を分けてください
5. 学習判定
   - 必須 / 軽微 の指摘ごとに、必要な場合だけ短く添えてください
   - `直す単位`: `case_fix / pattern_candidate / rule_return_candidate / preference / reject`
   - `再発根拠`: `なし / 同batch内 / 過去batch同型 / 既存rule未反映疑い`
   - `既存rule hit`: `あり / なし / 不明`
   - `戻し先候補`: `batch / reviewer_prompt / gold / rule / renderer / service_facts / validator / none`
6. rule に戻すべき再発パターン
   - `reply-only / common` を分けてください
   - 再発性が弱いものは無理に rule 化しないでください
7. 採点
   - 必要なら `直答 / scope順守 / 情報節約 / 自然さ / 次アクション明確さ` を短く評価してください
8. 最後に短くまとめ
   - `今回は止めてよい / 1〜2点だけ直す / まだ危ない` のどれかを、人間向けに短くまとめてください

まだ `rule / validator / gold / renderer / reviewer_prompt` には反映しないでください。監査結果を見て、再発性があるものだけ最小反映します。

---

## B01

stock_id: STK-032
状態: prequote
主質問タイプ: 普通の見積り相談・サブスクアップグレードDB未更新型
最初に答えるべき問い: Stripe側ではアップグレード済みで、イベントも受け取っているのにDBだけ更新されない件を見られるか。
選定意図: 情報量の多い通常 prequote で、イベント受信済み / DB未更新 / ダウングレード正常を拾えるかを見る。
r0_local_lint: OK

相手文:

> はじめまして。少し込み入った相談です。Stripeでサブスクリプションを管理しているんですが、お客さんがプランをアップグレードした際に、次の請求日まで旧プランのままになってしまいます。Stripeのダッシュボードではプラン変更は反映されているのに、うちのDB側が更新されていません。customer.subscription.updatedのイベントは受け取れてるっぽいです。ただ、イベントは受け取れてるのにDBが変わらないというのが不思議で。あと、ダウングレードの方は正常に動いてます。

返信候補:

ご相談ありがとうございます。

Stripe側ではアップグレードが反映され、customer.subscription.updated も受け取れているのに、DB側のプランが更新されない状態ですね。
ダウングレードは正常とのことなので、アップグレード時の更新分岐を中心に15,000円で確認できます。

まずは受信済みイベントの処理後に、DB更新がどこで止まっているかを確認します。

この内容で進める場合は、そのままご購入いただいて大丈夫です。必要情報がそろい次第、一次結果は48時間以内を目安にお返しします。

---

## B02

stock_id: STK-025
状態: prequote
主質問タイプ: 普通の見積り相談・非Stripe決済Webhook型
最初に答えるべき問い: StripeではなくPayPayオンライン決済のWebhook通知不具合でも見られるか。
選定意図: 非Stripe決済名が出た時に、PayPay全体ではなく Next.js側のWebhook/API受信処理へ範囲を閉じられるかを見る。
r0_local_lint: OK

相手文:

> すみません、Stripeじゃなくてpaypayオンライン決済を使ってるんですが、こちらのサービスで見てもらえますか？Next.jsで作ってます。決済完了のWebhook通知が来てないっぽくて。

返信候補:

ご相談ありがとうございます。

PayPayオンライン決済のWebhook通知が来ていない件ですね。

PayPay側そのものの仕様調査ではなく、Next.js側のWebhook/API受信処理として確認できる内容であれば見られます。
まずは通知を受けるAPIの場所と、表示されているエラーやログの有無を分かる範囲で送ってください。確認後、15,000円の範囲で進められるかをお返しします。

---

## B03

stock_id: STK-033
状態: prequote
主質問タイプ: 普通の見積り相談・緊急復旧時間質問型
最初に答えるべき問い: 今日の昼から決済が止まっている件を、何時間くらいで復旧できるか。
選定意図: 緊急・売上停止・復旧時間質問に、復旧保証を避けつつ直答できるかを見る。
r0_local_lint: OK

相手文:

> すみません、至急お願いしたいです。今日の昼からStripeの決済ができなくなっています。昨日まで普通に動いていました。何も変更していないのに急に動かなくなりました。お客さんが買い物できない状態で、売上が止まっています。何時間くらいで復旧できますか？

返信候補:

ご相談ありがとうございます。
お急ぎの状況は承知しました。

何時間で復旧できるかは、原因を確認してからでないとお約束できません。
ただ、今日からStripe決済が止まっている不具合として15,000円で対応できます。

購入後は、まず決済が止まっている箇所を優先して確認します。
一次結果は、必要情報がそろい次第48時間以内を目安にお返しします。これは復旧時間の保証ではなく、最初の確認結果の目安です。

この内容で進める場合は、そのままご購入ください。

---

## B04

stock_id: STK-010
状態: prequote
主質問タイプ: 普通の見積り相談・本番のみAPI timeout境界型
最初に答えるべき問い: 本番だけ500 / FUNCTION_INVOCATION_TIMEOUT が出るStripe関連APIを見られるか。
選定意図: 情報不足時に同義反復へ逃げず、本番のみ・Vercel timeout・Stripe API の具体情報を拾えるかを見る。
r0_local_lint: OK

相手文:

> はじめまして。Next.jsのApp Routerで作ったサイトで、APIルートが本番だけ500を返します。ローカルとプレビューでは問題なく動いています。Stripe関連のAPIです。Vercelのログを見ると「FUNCTION_INVOCATION_TIMEOUT」と出ていますが、ローカルでは数秒で返ってきます。

返信候補:

ご相談ありがとうございます。

本番だけ500で、Vercelのログに FUNCTION_INVOCATION_TIMEOUT が出ている状態ですね。
ローカルとプレビューでは数秒で返ってくるとのことなので、Stripe関連APIが本番環境でtimeoutしている原因を確認する形になります。

この不具合なら15,000円で対応できます。

まずはVercel側の実行時間制限と、Stripe APIの応答時間や呼び出し経路が本番でどう変わっているかを確認します。

この内容で進める場合は、そのままご購入ください。必要情報がそろい次第、一次結果は48時間以内を目安にお返しします。

---

## B05

stock_id: STK-083
状態: prequote
主質問タイプ: 普通の見積り相談・テスト環境から本番切替後の不具合型
最初に答えるべき問い: APIキーをliveに変え、Webhookも本番用にした後に動かなくなった件を見られるか。
選定意図: テスト/本番切替、APIキー、Webhook endpoint、エラーなしという具体情報を拾えるかを見る。
r0_local_lint: OK

相手文:

> Stripeのテスト環境では正常に動いているんですが、本番に切り替えたら動かなくなりました。切り替え時にやったことは、APIキーをliveに変えただけです。Webhookエンドポイントも本番用に変更しました。エラーメッセージは特に出ていないです。

返信候補:

ご相談ありがとうございます。

テスト環境では動いていて、APIキーをliveに変え、Webhookエンドポイントも本番用にした後に動かなくなった状態ですね。
エラーメッセージが出ていないとのことなので、本番キーとWebhook endpointの設定反映、処理が無言で止まっている箇所を確認します。

この不具合なら15,000円で対応できます。
APIキーやWebhook secretの値そのものは不要です。確認に必要な場合も、キー名や設定箇所が分かれば大丈夫です。

この内容で進める場合は、そのままご購入ください。必要情報がそろい次第、一次結果は48時間以内を目安にお返しします。

---

## B06

stock_id: STK-048
状態: purchased
主質問タイプ: 購入後・二重引き落とし/返金操作質問型
最初に答えるべき問い: 二重引き落としが今回依頼に含まれるか。Stripe上の返金処理操作も教えてもらえるか。
選定意図: 追加症状の範囲確認と、返金操作案内/返金実行代行の境界を分けられるかを見る。
r0_local_lint: OK

相手文:

> すみません、依頼した件とは別かもしれないんですが、今日新たにお客さんから「返金してほしい」と連絡が来ました。決済の不具合のせいで二重に引き落とされたみたいで。これも今回の依頼内容に含まれますか？返金処理の操作も教えてもらえますか？

返信候補:

ありがとうございます。
二重引き落としの件、確認しました。

今回の決済不具合と同じ原因で起きている内容であれば、このまま今回の件として確認します。
別の原因と分かった場合は、追加対応が必要かどうかを先にご相談します。

返金処理の操作については、原因の切り分け結果と合わせて、Stripe上で確認できる範囲の手順をお伝えします。
こちらで返金実行を代行するわけではありません。

まず今回の件とつながっているかを確認するので、二重に引き落とされた決済の日時・金額・決済IDなど、分かる範囲の情報を送ってください。カード情報や個人情報は不要です。

本日16:55までに、現時点の確認結果をお返しします。

---

## B07

stock_id: STK-045
状態: quote_sent
主質問タイプ: 見積り提案後・本番反映/デプロイ手順確認型
最初に答えるべき問い: 修正済みファイルは自分でVercelにデプロイするのか。手順も教えてもらえるか。
選定意図: 本番デプロイ代行不可と手順提供の代替を、購入前に自然に返せるかを見る。
r0_local_lint: OK

相手文:

> 見積りの件、検討中です。1つ聞き忘れたんですが、修正してもらったファイルは自分でVercelにデプロイする形ですよね？デプロイのやり方が分からないんですが、手順も教えてもらえますか？

返信候補:

ご連絡ありがとうございます。
ご自身で反映する場合のサポート範囲ですね。

はい、今回の提案では、修正済みファイルをお渡しし、本番反映は依頼者様側で行っていただく形です。
Vercelのデプロイ手順も、修正ファイルと一緒に分かる形でお渡しします。

この内容で問題なければ、そのままご購入いただいて大丈夫です。
